{
  "name": "@lab/management-system-contract-test",
  "version": "0.1.0",
  "description": "Lab Management System — contract conformance suite. Black-box HTTP against every backend; asserts they are indistinguishable to the frontend (ADR-0015).",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "axios": "^1.19.0",
    "axios-cookiejar-support": "^5.0.3",
    "tough-cookie": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
